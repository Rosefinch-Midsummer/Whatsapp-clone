const authConfig = {
    providers: [
        {
            domain: "https://singular-quagga-65.clerk.accounts.dev",
            applicationID: "convex",
        },
    ]
};
export default authConfig;